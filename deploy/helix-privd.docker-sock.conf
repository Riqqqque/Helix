# Drop-in for hosts whose helix-privd unit customized ReadWritePaths.
# Copy to /etc/systemd/system/helix-privd.service.d/docker-sock.conf
# then daemon-reload and restart helix-privd. Helix-only; does not restart
# game containers.
[Service]
ReadWritePaths=/run/docker.sock /var/run/docker.sock
