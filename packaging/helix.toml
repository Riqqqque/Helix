# Remote binds are rejected until TLS, trusted-proxy handling, and the reviewed
# remote deployment security boundary are implemented and validated.
[server]
listen = "127.0.0.1:8080"

[paths]
data_dir = "/var/lib/helix"
web_root = "/usr/share/helix/web"
