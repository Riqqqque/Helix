# Type Name  ID  Description                    Home            Shell
# Shell "-" uses this distro's nologin path instead of hard-coding
# /usr/sbin/nologin, which Fedora/Arch often keep at /sbin or /usr/bin.
u      helix -   "Helix control plane service" /var/lib/helix  -
